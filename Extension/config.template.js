/**
 * SOTI AI Analyser — Configuration Template
 * ==========================================
 * This is the TEMPLATE file. It IS committed to version control.
 *
 * Setup Instructions for Engineers:
 * 1. Copy this file and name the copy: config.js
 * 2. Fill in the apiKey value in config.js with the key provided by your administrator.
 * 3. Never rename config.js back to config.template.js or commit config.js to git.
 *
 * The actual config.js is excluded from version control via .gitignore.
 */
window.SOTI_AI_CONFIG = {
    apiKey: 'YOUR_OPENROUTER_API_KEY_HERE'
};
